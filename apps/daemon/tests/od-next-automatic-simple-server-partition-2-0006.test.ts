import { registerOdNextAutomaticSimpleServerTests } from './od-next-automatic-simple-server.suite.js';

// The suffix keeps this file near the middle of daemon shard 2's SHA-1 range.
registerOdNextAutomaticSimpleServerTests(2);
