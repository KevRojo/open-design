// Temporary source-boundary probe for the release-beta convergence experiment.
// It is deliberately side-effect free and will be removed after the scenario matrix.
export const RELEASE_BETA_DAEMON_SOURCE_PROBE = true;
